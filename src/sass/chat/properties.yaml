name: Chat
components:
  - name: Base Theme
    $ref: base.scss
  - name: Themes
    oneof: true
    selected: 2
    components:
      - $ref: alpha
      - $ref: blur
      - name: Transparent
        $ref: transparent.scss
